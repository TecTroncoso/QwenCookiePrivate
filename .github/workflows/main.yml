# .github/workflows/main.yml (Versión Adaptada para Render)
name: Weekly Qwen Cookie Refresh & Update Render
on:
  schedule:
    - cron: "0 3 * * 0" # Todos los domingos a las 3:00 AM UTC
  workflow_dispatch: # Permite ejecución manual
jobs:
  refresh-and-update:
    name: Refresh Cookie and Update Render Env Var
    runs-on: ubuntu-latest
    # No necesitas 'environment' a menos que lo uses para protección de secrets
    env:
      # Secretos necesarios para el script y la API de Render
      RENDER_API_KEY: ${{ secrets.RENDER_API_KEY }}
      RENDER_SERVICE_ID: ${{ secrets.RENDER_SERVICE_ID }}
      USERNAME: ${{ secrets.USERNAME }}
      PASSWORD: ${{ secrets.PASSWORD }}
    steps:
      - name: Checkout Repository Code
        uses: actions/checkout@v4

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install Dependencies and Tools
        run: |
          python -m pip install --upgrade pip
          # jq es esencial para procesar la respuesta JSON de la API de Render
          sudo apt-get update && sudo apt-get install -y xvfb jq curl
          pip install -r requirements.txt

      - name: Run Selenium Script to Get Cookies
        id: get-cookies
        run: |
          echo "🔄 Ejecutando script de Selenium para obtener cookies..."
          xvfb-run -a python main.py
          
          if [ ! -s cookies.json ]; then
            echo "::error::El archivo 'cookies.json' no fue creado o está vacío."
            exit 1
          fi
          
          echo "✅ Archivo 'cookies.json' generado correctamente."
          echo "📄 Contenido del archivo (primeras líneas):"
          head -n 3 cookies.json

      - name: Update Env Var on Render and Trigger Redeployment
        run: |
          echo "🔄 Codificando cookies a Base64..."
          B64_COOKIES=$(base64 -w 0 cookies.json)
          
          if [ -z "$B64_COOKIES" ]; then
            echo "::error::No se pudieron codificar las cookies."
            exit 1
          fi
          
          echo "✅ Cookies codificadas (longitud: ${#B64_COOKIES} caracteres)"
        
          # --- INTERACCIÓN CON LA API DE RENDER ---

          # 1. OBTENER EL ID DE LA VARIABLE DE ENTORNO
          # La API de Render necesita el ID específico de la variable, no solo su nombre.
          echo "🔍 Buscando el ID de la variable de entorno 'QWEN_COOKIES_JSON_B64' en Render..."
          
          ENV_VARS_JSON=$(curl -s -H "Authorization: Bearer $RENDER_API_KEY" \
                               -H "Accept: application/json" \
                               "https://api.render.com/v1/services/$RENDER_SERVICE_ID/env-vars")

          ENV_VAR_ID=$(echo "$ENV_VARS_JSON" | jq -r '.[] | select(.key == "QWEN_COOKIES_JSON_B64") | .id')

          if [ -z "$ENV_VAR_ID" ] || [ "$ENV_VAR_ID" = "null" ]; then
            echo "::error::No se pudo encontrar el ID de la variable de entorno 'QWEN_COOKIES_JSON_B64'."
            echo "Asegúrate de que la variable exista en tu servicio de Render y que RENDER_SERVICE_ID sea correcto."
            exit 1
          fi
          
          echo "✅ ID de la variable de entorno encontrado: $ENV_VAR_ID"

          # 2. ACTUALIZAR LA VARIABLE DE ENTORNO CON LAS NUEVAS COOKIES
          # Usamos una petición PUT al endpoint específico de la variable.
          echo "🔄 Actualizando la variable de entorno en Render..."
          
          curl --fail -X PUT "https://api.render.com/v1/services/$RENDER_SERVICE_ID/env-vars/$ENV_VAR_ID" \
            -H "Authorization: Bearer $RENDER_API_KEY" \
            -H "Content-Type: application/json" \
            -H "Accept: application/json" \
            -d "{\"value\": \"$B64_COOKIES\"}"

          echo "✅ Variable de entorno actualizada correctamente."
          
          # 3. INICIAR UN NUEVO DESPLIEGUE PARA APLICAR LOS CAMBIOS
          echo "🚀 Iniciando redespliegue del servicio en Render..."
          
          curl --fail -X POST "https://api.render.com/v1/services/$RENDER_SERVICE_ID/deploys" \
            -H "Authorization: Bearer $RENDER_API_KEY" \
            -H "Content-Type: application/json" \
            -H "Accept: application/json" \
            -d '{"clearCache": "no"}'
            
          echo "✅ Redespliegue iniciado en Render."
          echo "🎯 Proceso completado exitosamente!"

      - name: Cleanup
        if: always()
        run: |
          echo "🧹 Limpiando archivos temporales..."
          rm -f cookies.json
          echo "✅ Cleanup completado."
